# Immich OIDC (PocketID) — store in Vault, not here
# Create OIDC client at https://id.mol.la/settings/admin/oidc-clients
# Redirect URIs: https://photos.mol.la/auth/login, https://photos.mol.la/user-settings, app.immich:///oauth-callback
# Then: vault kv put kv/homelab/data/immich_oidc client_id="YOUR_CLIENT_ID" client_secret="YOUR_CLIENT_SECRET"
