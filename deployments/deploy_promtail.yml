---
# Deploy Promtail (ships Docker container logs to Loki on monitoring LXC).
# Usage: ./lab deploy promtail
#   ansible-playbook deployments/deploy_promtail.yml --limit shobd
- name: Deploy Promtail
  hosts: "{{ target_hosts | default('timothy_host') }}"
  become: true
  roles:
    - promtail
