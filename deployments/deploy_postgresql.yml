---
# Deploy PostgreSQL — passwords fetched from Vault.
# Usage: ansible-playbook deployments/deploy_postgresql.yml -e "@vars/vault_auth_vars.yml" -e "@vars/postgresql_dbs.yml"
- name: Deploy PostgreSQL
  hosts: postgresql_host
  become: true
  roles:
    - postgresql
