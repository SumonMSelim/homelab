---
# Deploy PostgreSQL — passwords fetched from Vault.
# Usage: ansible-playbook deployments/deploy_postgresql.yml -e "@vars/vault_auth_vars.yml"
- name: Deploy PostgreSQL
  hosts: postgresql_host
  become: true
  vars_files:
    - ../vars/postgresql_apps.yml
  roles:
    - postgresql
