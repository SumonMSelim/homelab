name: OCI Experiment — CPU Baseline

# Long-lived 16 OCPU / 128 GB A1.Flex running qwen3:72b for 2-week trial.
# Cost: ~€101 over 14 days.
# Hard auto-destroy: 2026-05-20 02:00 UTC (trial expires ~2026-05-21).

on:
  workflow_dispatch:
    inputs:
      action:
        description: "Action"
        required: true
        default: plan
        type: choice
        options: [plan, apply, destroy]
  schedule:
    - cron: "0 2 20 5 *"   # 2026-05-20 02:00 UTC hard destroy

permissions:
  contents: read

env:
  TF_VERSION: "1.15.1"
  TF_DIR: cloud/oci/experiment/terraform/envs/cpu-baseline
  TF_BACKEND_KEY: "oci-exp-cpu/terraform.tfstate"
  TF_BACKEND_BUCKET: ${{ vars.OCI_STATE_BUCKET }}
  TF_BACKEND_REGION: ${{ vars.OCI_REGION }}
  TF_BACKEND_ENDPOINT: "https://${{ vars.OCI_STATE_NAMESPACE }}.compat.objectstorage.${{ vars.OCI_REGION }}.oraclecloud.com"

jobs:
  terraform:
    name: ${{ github.event_name == 'schedule' && 'Auto-destroy (trial end)' || format('CPU {0}', github.event.inputs.action) }}
    runs-on: ubuntu-24.04-arm
    if: github.event_name == 'schedule' || github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Write OCI key
        run: |
          mkdir -p ~/.oci
          echo "${{ secrets.OCI_PRIVATE_KEY_CONTENT }}" > ~/.oci/oci_api_key.pem
          chmod 600 ~/.oci/oci_api_key.pem

      - name: Terraform Init
        working-directory: ${{ env.TF_DIR }}
        env:
          AWS_REQUEST_CHECKSUM_CALCULATION: when_required
          AWS_RESPONSE_CHECKSUM_VALIDATION: when_required
        run: |
          terraform init \
            -backend-config="bucket=${TF_BACKEND_BUCKET}" \
            -backend-config="key=${TF_BACKEND_KEY}" \
            -backend-config="region=${TF_BACKEND_REGION}" \
            -backend-config="endpoints={s3=\"${TF_BACKEND_ENDPOINT}\"}" \
            -backend-config="access_key=${{ secrets.OCI_STATE_ACCESS_KEY }}" \
            -backend-config="secret_key=${{ secrets.OCI_STATE_SECRET_KEY }}" \
            -backend-config="skip_region_validation=true" \
            -backend-config="skip_credentials_validation=true" \
            -backend-config="skip_metadata_api_check=true" \
            -backend-config="skip_requesting_account_id=true" \
            -backend-config="use_path_style=true" \
            -backend-config="skip_s3_checksum=true"

      - name: Terraform Plan
        if: github.event_name != 'schedule'
        working-directory: ${{ env.TF_DIR }}
        env: &tf_vars
          TF_VAR_tenancy_ocid: ${{ secrets.OCI_TENANCY_OCID }}
          TF_VAR_user_ocid: ${{ secrets.OCI_USER_OCID }}
          TF_VAR_fingerprint: ${{ secrets.OCI_FINGERPRINT }}
          TF_VAR_private_key_content: ${{ secrets.OCI_PRIVATE_KEY_CONTENT }}
          TF_VAR_region: ${{ vars.OCI_REGION }}
          TF_VAR_compartment_ocid: ${{ secrets.OCI_COMPARTMENT_OCID }}
          TF_VAR_availability_domain: ${{ vars.OCI_AVAILABILITY_DOMAIN }}
          TF_VAR_ssh_public_key: ${{ secrets.OCI_SSH_PUBLIC_KEY }}
          TF_VAR_tailscale_auth_key: ${{ secrets.TAILSCALE_AUTH_KEY_OCI }}
          AWS_REQUEST_CHECKSUM_CALCULATION: when_required
          AWS_RESPONSE_CHECKSUM_VALIDATION: when_required
        run: terraform plan -out=tfplan -input=false

      - name: Terraform Apply
        if: github.event_name != 'schedule' && github.event.inputs.action == 'apply'
        working-directory: ${{ env.TF_DIR }}
        env: *tf_vars
        run: terraform apply -auto-approve -input=false tfplan

      - name: Terraform Destroy
        if: github.event_name == 'schedule' || github.event.inputs.action == 'destroy'
        working-directory: ${{ env.TF_DIR }}
        env: *tf_vars
        run: terraform destroy -auto-approve -input=false

      - name: Summary
        if: always()
        run: |
          ACTION="${{ github.event_name == 'schedule' && 'auto-destroy' || github.event.inputs.action }}"
          echo "### CPU Baseline — $ACTION" >> "$GITHUB_STEP_SUMMARY"
          echo "Ran at $(date -u)" >> "$GITHUB_STEP_SUMMARY"
